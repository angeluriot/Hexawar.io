import * as UserConnection from './connection.js';

export function login_events()
{/*
	const username_input = document.querySelector('.username_input') as HTMLInputElement;
	const password_input = document.querySelector('.password_input') as HTMLInputElement;
	const password_repeat_input = document.querySelector('.password_repeat_input') as HTMLInputElement;
	const register_button = document.querySelector('.register_button') as HTMLButtonElement;
	const login_button = document.querySelector('.login_button') as HTMLButtonElement;
	const logout_button = document.querySelector('.logout_button') as HTMLButtonElement;

	register_button.addEventListener('click', (e) =>
	{
		e.preventDefault();
		UserConnection.register(username_input.value, password_input.value);
	});

	login_button.addEventListener('click', (e) =>
	{
		e.preventDefault();
		UserConnection.login(username_input.value, password_input.value);
	});

	logout_button.addEventListener('click', (e) =>
	{
		e.preventDefault();
		UserConnection.logout();
	});*/
}
